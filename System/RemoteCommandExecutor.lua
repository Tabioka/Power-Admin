-- RemoteCommandExecutor.lua
local function executeRemoteCommand(command, args)
    -- Execute a command from an external service (like Discord)
end

return {
    executeRemoteCommand = executeRemoteCommand
}
